server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/alerta_vecinal_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
